export const selectCreateState = (state) => state.journalEditor.create;

export const selectDeleteState = (state) => state.journalEditor.delete;

export const selectUpdateState = (state) => state.journalEditor.update;
